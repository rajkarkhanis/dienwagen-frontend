import { VehicleModel } from "./vehicle-model";

export class VehicleEngine {
    public engineId: number = 0;
    public engineName: string = "";
    public vehicleModel: VehicleModel = new VehicleModel;
    public enginePrice: number = 0;
}
